#include<stdio.h>
#include<windows.h>

int comparaArquivo(char arq1[20], char arq2[20]) {
	FILE*Ptr1 = fopen(arq1,"r");
	FILE*Ptr2 = fopen(arq2,"r");
	while(!feof(PtrAp1)) {
		if(getc(Ptr1)!=getc(Ptr2))
			return		
	}
}
